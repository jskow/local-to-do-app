<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Where are you,'+' right now'+'?' %><br>
    <%= f.text_field :address %> <br> 

    <%= f.label 'How much $$$?' %> <br>
    <%= f.label 'Free' %>
    <%= f.radio_button :cost, "low" %>
    <%= f.label 'A bit' %>
    <%= f.radio_button :cost, "mid" %>
    <%= f.label 'A lot' %>
    <%= f.radio_button :cost, "high" %> <br>

    <%= f.label 'How many people?' %> <br>
    <%= f.label 'A couple' %>
    <%= f.radio_button :group_size, "low" %>
    <%= f.label 'A few' %>
    <%= f.radio_button :group_size, "mid" %>
    <%= f.label 'A lot' %>
    <%= f.radio_button :group_size, "high" %> <br>

    <%= f.label 'What ages?' %> <br>
    <%= f.label 'Under 21' %>
    <%= f.radio_button :age, "low" %>
    <%= f.label '21 & up' %>
    <%= f.radio_button :age, "mid" %>
    <%= f.label 'Family' %>
    <%= f.radio_button :age, "high" %> <br>
  </div>

  <div class="actions">
    <%= f.submit 'Submit Location', :class => 'btn-large btn-success' %>
  </div>
<% end %>
